# 마이링크 (MyLink) 제품 사양서 (PRD)

---

**버전: 0.4 (링크 관리 구현 반영본)**

**작성일: 2026-05-26**

**상태: 초안 (Draft)**

---

## 1. 프로젝트 개요

---

### 1.1. 프로젝트명

**마이링크 (MyLink)**

### 1.2. 목적

개발자와 크리에이터가 자신의 작업물, 포트폴리오, 소셜 미디어 등 흩어진 디지털 자산을 통합하여 관리하고 공유할 수 있는 서비스를 제공한다.

### 1.3. 대상 사용자 (Target Audience)

- **Primary:** 자신의 기술 블로그, Github 작업물, 프로젝트 데모 등을 효과적으로 보여주고 싶은 개발자.
- **Secondary:** 포트폴리오 관리가 필요한 크리에이터.

---

## 2. 핵심 기능 목록 (Feature List)

---

### 2.1. 필수 기능 (Must-Have / MVP)

1. **사용자 인증 (Auth)**
   - Firebase Authentication 기반 **Google 소셜 로그인**.

2. **프로필 관리**
   - **DisplayName 기반 URL:** 별도의 `username` 필드 없이 `displayName`을 URL slug로 사용 (예: `mylink.com/@displayName`).
   - **DisplayName 초기값:** Google 로그인 이메일의 `@` 앞부분을 가져와 설정 (예: `devkim@gmail.com` -> `devkim`).
   - **연필 아이콘 편집 UI:** 수정 가능한 정보 옆에 연필 아이콘을 표시하고, 아이콘 클릭 시 즉시 수정 모드로 전환.
   - **인라인 편집:** 별도의 수정 폼/모달 없이 연필 아이콘 또는 텍스트를 클릭하여 즉시 수정.
   - 수정 가능 항목: 닉네임, 한 줄 소개(Bio). (이미지 업로드 기능 없음 - Google 프로필 이미지 사용).

3. **링크 블록 관리**
   - 링크 추가/수정/삭제.
   - **추가 다이얼로그:** 목록 상단의 강조된 `새로운 링크 추가하기` 버튼을 선택하면 제목과 URL 입력 다이얼로그 표시.
   - **상시 표시 관리 버튼:** 각 링크 카드에 연필 아이콘 수정 버튼과 휴지통 아이콘 삭제 버튼을 항상 표시.
   - **인라인 편집:** 연필 아이콘 클릭 시 링크 제목 및 URL을 카드 안에서 바로 수정.
   - **공통 입력 검증:** 링크 추가와 수정에 동일한 제목 필수값 및 URL 형식 검증 적용.
   - **삭제 확인 모달:** 링크 이름과 되돌릴 수 없다는 경고를 확인한 뒤 삭제 실행.
   - **파비콘 자동 연동 (Google API):** 링크 URL 입력 시 Google Favicon API를 사용하여 아이콘 자동 표시.

4. **퍼블릭 페이지 (뷰어)**
   - 방문자가 보는 실제 프로필 페이지.
   - 반응형 디자인 (모바일 최적화).
   - shadcn/ui 기반의 깔끔한 UI.

### 2.2. 추후 제공 예정 (Future Plan)

- **링크 클릭 조회수:** 개별 링크의 클릭 횟수 카운팅 기능.

### 2.3. 제외 기능 (Out of Scope)

- **이미지 업로드:** 커스텀 프로필 이미지 업로드 불가 (Google 계정 이미지 사용).
- **테마 시스템:** 다크/라이트 모드 또는 컬러 커스터마이징 없음.
- 링크 활성화/비활성화 토글.
- 링크 드래그 앤 드롭 순서 변경.
- 개발자 특화 카드.
- 모바일 뷰 미리보기.

---

## 3. 기능 상세 설명

---

### 3.1. 인증 (Authentication)

- **진입점:** 메인 랜딩 페이지의 "Google로 시작하기" 버튼.
- **로직:** Firebase Auth.
- **초기값:** Google 로그인 이메일의 `@` 앞부분을 `displayName` 초기값으로 설정하고, Google 계정 사진(PhotoURL)을 프로필 이미지로 설정.

### 3.2. 대시보드 (Admin) - 인라인 에디팅 중심

- **프로필 영역:**
  - 프로필 사진(Google URL, 수정 불가), 닉네임(수정 가능), 소개글(수정 가능) 표시.
  - 닉네임/소개글 옆에 연필 아이콘을 표시하여 수정 가능한 정보임을 명확히 안내.
  - 닉네임/소개글의 연필 아이콘 또는 텍스트 클릭 시 `contenteditable` 혹은 `input`으로 전환되어 즉시 수정 (Inline Edit).
  - **중복 검사:** `displayName` 수정 시 유일성(Uniqueness) 검사 필요 (URL slug로 사용되므로).

- **링크 목록 영역:**
  - `새로운 링크 추가하기` 버튼: 목록 상단에 한 개의 추가 아이콘과 문구만 표시하는 단순한 강조 버튼으로 배치하고, 클릭 시 링크 추가 다이얼로그를 연다.
  - 추가 중에는 다이얼로그를 열린 상태로 유지하고, 다이얼로그 내부 추가 버튼에 회전 로딩 아이콘만 표시한다.
  - 추가 저장이 성공하면 다이얼로그를 닫고, 실패하면 다이얼로그에서 오류를 표시한다.
  - 리스트 아이템:
    - 아이콘: URL 입력 시 `https://www.google.com/s2/favicons?domain={url}` 형식을 이용하여 자동 렌더링.
    - 연필 아이콘: 항상 표시되며, 클릭 시 제목 및 URL 인라인 수정 모드로 전환한다.
    - 수정 저장: 링크 추가와 동일한 입력 검증을 통과한 뒤 Firestore 문서를 갱신한다.
    - 삭제 버튼: 항상 표시되며, 클릭 시 삭제 확인 모달을 연다.
    - 삭제 확인 모달: 제목 `정말 삭제하시겠습니까?`, 링크 이름, 빨간색 경고 `이 작업은 되돌릴 수 없습니다`, `취소` 및 `삭제하기` 버튼을 표시한다.

### 3.3. 디자인 시스템 (UI/UX)

- **프레임워크:** shadcn/ui.
- **인터랙션:** 빠르고 간편한 수정 경험을 위해 연필 아이콘 기반 인라인 편집과 명시적인 저장/취소 동작을 제공한다.
- **위험 동작 안내:** 삭제는 확인 모달과 빨간색 경고 문구를 통해 의도하지 않은 삭제를 방지한다.

### 3.4. 링크 데이터 동기화

- 현재 프로토타입의 링크 저장 경로는 `users/anonymous/links`이다.
- 페이지 로드 시 Firestore에서 링크를 불러오고 `createdAt` 기준 최신순으로 표시한다.
- 링크 추가는 새 문서 생성 이벤트를 구독하여 목록에 즉시 반영한다.
- 링크 수정 및 삭제는 작업 성공 후 Firestore에서 목록을 다시 조회하여 화면에 반영한다.

---

## 4. 데이터베이스 모델링 (NoSQL - Firestore)

---

### 4.1. Users Collection

```json
{
  "uid": "google_uid_123",
  "email": "dev_kim@gmail.com",
  "displayName": "dev_kim",
  "photoURL": "https://lh3.googleusercontent.com/...",
  "bio": "Frontend Developer",
  "createdAt": "timestamp"
}
```

Note: `displayName`을 URL slug로 사용하므로 유일성을 보장하기 위한 별도 인덱스나 로직이 필요함.

### 4.2. Links Sub-collection (현재 구현: users/anonymous/links)

```json
{
  "id": "link_uuid",
  "title": "My Blog",
  "url": "https://blog.example.com",
  "createdAt": "timestamp",
  "updatedAt": "timestamp"
}
```

Note: 현재 인증 연결 전 프로토타입은 `users/anonymous/links`를 사용한다. 인증 기능이 연결되면 동일한 하위 컬렉션 구조를 사용자 문서(`users/{uid}/links`) 아래에 적용할 수 있다.

Note: 파비콘은 DB에 저장하지 않고 클라이언트에서 `url`을 기반으로 동적 생성한다.
